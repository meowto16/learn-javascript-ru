/**
 * Почему остаётся "aaa"? (важность: 1)
 *
 * Запустите этот пример. Почему вызов remove не удалил текст "aaa"?
 *
 * <table id="table">
 *   aaa
 *   <tr>
 *     <td>Тест</td>
 *   </tr>
 * </table>
 *
 * <script>
 *   alert(table); // таблица, как и должно быть
 *
 *   table.remove();
 *   // почему в документе остался текст "ааа"??
 * </script>
 */

// Решение: потому что HTML некорректный и текст сдвигается до <table>