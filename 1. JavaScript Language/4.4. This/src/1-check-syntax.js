/**
 * Проверка синтаксиса (важность: 2)
 *
 * Каким будет результат выполнения этого кода?
 */

let user = {
  name: "Джон",
  go: function() { alert(this.name) }
}

(user.go)()

// Решение: будет ошибка, так как нет точки с запятой после объявления user
