/**
 * Какие значения выведет цикл while? (важность: 4)
 *
 * Для каждого цикла запишите, какие значения он выведет. Потом сравните с ответом.
 * Оба цикла выводят alert с одинаковыми значениями или нет?
 */

// 1. Префиксный вариант ++i:

let i = 0;
while (++i < 5) alert( i );

// 2. Постфиксный вариант i++

let j = 0;
while (j++ < 5) alert( j );

/**
 * Решение:
 * 1-ый цикл начнет итерироваться с единицы, выведет 1,2,3,4
 * 2-ой цикл выведет 1,2,3,4,5
 */

