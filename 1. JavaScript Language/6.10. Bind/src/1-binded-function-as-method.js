/**
 * Связанная функция как метод (важность: 5)
 *
 * Что выведет функция?
 */

function f() {
  alert(this);
}

let user = {
  g: f.bind(null)
};

user.g();

// Решение: функция выведет null, так как привязанный однажды контекст уже нельзя изменить.
