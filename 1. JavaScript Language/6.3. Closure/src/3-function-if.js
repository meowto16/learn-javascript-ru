/**
 * Функция в if
 *
 * Посмотрите на код. Какой будет результат у вызова на последней строке?
 */

let phrase = "Hello";

if (true) {
  let user = "John";
  
  function sayHi() {
    alert(`${phrase}, ${user}`);
  }
}

sayHi();

// Решение: должна быть ошибка, так как функция объявлена в лексическом окружении тела if'а
