/**
 * Что выведет этот код? (важность: 5)
 *
 * Что выведет код ниже?
 */

alert( null || 2 && 3 || 4 );
// Решение:
// У && выше приоритет, поэтому сначала вычислится 2 && 3, вернет 3
// Затем вычислится null || 3, вернет 3, так как 3 truthy-значение
// Затем сработает alert(3), до 4 дело не дойдет
