/**
 * Что выведет alert (ИЛИ)? (важность: 3)
 *
 * Что выведет код ниже?
 */

alert( alert(1) || 2 || alert(3) );
// Решение:
// Сработает alert(1) -> вернет undefined
// Затем вернет 2 в алерте
// alert(3) не сработает, так как уже нашлось truthy-значение 2
