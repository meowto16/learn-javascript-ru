/**
 * Что выведет alert (И)? (важность: 3)
 *
 * Что выведет код ниже?
 */

alert( alert(1) && alert(2) );
// Решение:
// Так как alert возвращает undefined, то выведет сначала 1, затем `undefined`
// До alert(2) выполнение не дойдет
