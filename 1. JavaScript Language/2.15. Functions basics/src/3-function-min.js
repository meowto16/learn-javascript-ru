/**
 * Функция min(a, b) (важность: 1)
 *
 * Напишите функцию min(a,b), которая возвращает меньшее из чисел a и b.
 * Пример вызовов:
 */

function min(a, b) {
  if (a < b) return a
  if (b <= a) return b
}
