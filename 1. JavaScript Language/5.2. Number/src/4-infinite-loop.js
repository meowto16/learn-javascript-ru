/**
 * Бесконечный цикл по ошибке (важность: 4)
 *
 * Этот цикл – бесконечный. Он никогда не завершится, почему?
 */

let i = 0;
while (i != 10) {
  i += 0.2;
}

// Решение: потому что опять потеря точности
// Исправить можно так:

while(+i.toFixed(1) !== 10) {
  i += 0.2
  console.log(i)
}
